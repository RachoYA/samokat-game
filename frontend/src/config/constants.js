// Игровые константы

export const GRID_SIZE = 20; // Размер игровой сетки (20x20)
export const CELL_SIZE = 60; // Размер одной клетки в пикселях

// Настройки игры
export const MAX_CARDS = 3; // Количество карточек для выбора
export const RECENT_BUILDINGS_LIMIT = 20; // Ограничение истории для баланса (увеличено для лучшего баланса)

// Настройки анимации
export const CAR_COUNT = 20; // Количество машинок
export const PEOPLE_COUNT = 25; // Количество людей

// Настройки дорог - РЕГУЛЯРНАЯ СЕТКА для обеспечения доступа к зданиям
export const ROAD_GRID_STEP = 3; // Дорога каждые 3 клетки (гарантирует доступ к любому зданию)

// Скорости движения
export const CAR_SPEED_MIN = 0.02;
export const CAR_SPEED_MAX = 0.04;
export const PERSON_SPEED_MIN = 0.01;
export const PERSON_SPEED_MAX = 0.02;

// ============================================================================
// 🧭 ОРИЕНТАЦИЯ МАШИНЫ - СМОТРИТЕ КОМПАС В ПРАВОМ НИЖНЕМ УГЛУ ИГРЫ!
// ============================================================================
// Компас показывает направления:
//   N (Север/Синий)   = Z- = Вверх на экране
//   S (Юг/Зеленый)    = Z+ = Вниз на экране
//   E (Восток/Красный) = X+ = Вправо на экране
//   W (Запад/Желтый)  = X- = Влево на экране
//
// ТЕКУЩАЯ НАСТРОЙКА: Передняя часть машины смотрит на ВОСТОК (E / вправо)
//
// ПРОВЕРЕНО:
//   ✓ На ВОСТОК (E) - машина едет прямо
//   ✗ На ЗАПАД (W) - едет задом, нужен разворот
//   ✗ На ЮГ (S) - едет диагонально, надо повернуть на 90° на запад
//   ✗ На СЕВЕР (N) - едет диагонально, надо повернуть на 90° на восток
// ============================================================================

// Базовая ориентация моделей (в радианах)
export const CAR_BASE_ROTATION_X = -Math.PI / 2; // -90° (встать на колёса)
export const CAR_BASE_ROTATION_Y = 0; // 0° = ВОСТОК (E) - передняя часть вправо

// Углы поворота для направлений движения (скорректировано на основе тестов)
export const CAR_ROTATION_RIGHT = 0;            // Восток (E) - прямо ✓
export const CAR_ROTATION_LEFT = Math.PI;       // Запад (W) - разворот на 180°
export const CAR_ROTATION_UP = Math.PI / 2;     // Север (N) - поворот на +90° (на восток)
export const CAR_ROTATION_DOWN = -Math.PI / 2;  // Юг (S) - поворот на -90° (на запад)
