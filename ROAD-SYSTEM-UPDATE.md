# 🛣️ Обновление системы дорог и правил строительства

## ✅ Реализованные изменения:

### 1. **Размер поля: 16x16** (было 32x32)
`frontend/src/config/constants.js`:
```javascript
GRID_SIZE = 16
```

### 2. **РЕГУЛЯРНАЯ СЕТКА ДОРОГ** (вместо случайной генерации)

#### Новая константа:
```javascript
ROAD_GRID_STEP = 3 // Дорога каждые 3 клетки
```

#### Принцип работы:
- Дороги генерируются **регулярной сеткой** с шагом 3 клетки
- **Для 16x16**: получается 6 горизонтальных и 6 вертикальных дорог
- Дороги проходят по координатам: 0, 3, 6, 9, 12, 15
- Это создает **кварталы 2x2** клетки между дорогами

#### Преимущества:
✅ **Гарантированный доступ** к любой клетке  
✅ **Равномерное распределение** дорог по полю  
✅ **Предсказуемая структура** города  
✅ **Оптимальное соотношение** дорог и зданий (примерно 40% дорог, 60% участков)

### 3. **ПРАВИЛО: К ЗДАНИЮ ВСЕГДА ДОЛЖНА БЫТЬ ДОРОГА**

#### Новая функция проверки:
`frontend/src/systems/GridSystem.js`:
```javascript
hasRoadAccess(x, y) {
    // Проверяет, есть ли дорога хотя бы в одной соседней клетке
    // (Север, Юг, Запад, Восток)
}
```

#### Что проверяется:
1. ✅ Клетка не является дорогой (нельзя строить на дороге)
2. ✅ Хотя бы одна соседняя клетка - дорога (доступ обеспечен)

#### Интеграция в игру:
`frontend/src/game-engine.js`:
```javascript
async onCellClick(x, y) {
    // 1. Проверка что клетка не занята
    if (this.gridSystem.isCellOccupied(x, y)) return;
    
    // 2. НОВАЯ ПРОВЕРКА - есть ли доступ к дороге
    if (!this.gridSystem.hasRoadAccess(x, y)) {
        console.warn('⚠️ Нельзя строить: рядом нет дороги!');
        return;
    }
    
    // 3. Строим здание
    ...
}
```

### 4. **ВИЗУАЛЬНАЯ ИНДИКАЦИЯ**

Клетки теперь окрашены по-разному:

- 🟩 **Зеленые клетки** (светло-зеленые) - можно строить (есть доступ к дороге)
- ⬜ **Серые клетки** - нельзя строить (нет доступа к дороге)
- 🟫 **Дорожные клетки** - текстурированные дороги

### 5. **ЦЕНТРИРОВАНИЕ**

Камера автоматически центрируется:
```javascript
camera.position = new BABYLON.Vector3(gridCenter, 0, gridCenter)
camera.radius = GRID_SIZE * 3.5 // Для 16x16 = 56
```

### 6. **Адаптированное количество NPC**
```javascript
CAR_COUNT = 10    (было 15)
PEOPLE_COUNT = 15 (было 20)
```

## 🎯 Результаты:

### До изменений:
- Поле: 32x32
- Дороги: Случайные (5-8 горизонтальных и вертикальных)
- Правило строительства: Можно строить везде
- Проблема: Здания могли быть недоступны

### После изменений:
- Поле: 16x16 ✅
- Дороги: Регулярная сетка (6x6) каждые 3 клетки ✅
- Правило строительства: **Только рядом с дорогой** ✅
- Результат: Гарантированный доступ к каждому зданию ✅

## 📊 Структура города:

```
Сетка 16x16 с дорогами каждые 3 клетки:

0  - - - 3  - - - 6  - - - 9  - - - 12 - - - 15
|  [   ]  |  [   ]  |  [   ]  |  [   ]  |  [   ]  |
|  [   ]  |  [   ]  |  [   ]  |  [   ]  |  [   ]  |
|  [   ]  |  [   ]  |  [   ]  |  [   ]  |  [   ]  |
3  - - - - - - - - - - - - - - - - - - - - - - - -
|  [   ]  |  [   ]  |  [   ]  |  [   ]  |  [   ]  |
...
```

Где:
- `—` и `|` — дороги
- `[   ]` — участки для строительства
- Каждый участок граничит хотя бы с одной дорогой

## 🔍 Логи в консоли:

При запуске игры:
```
GridSystem: Creating game grid with regular road network...
GridSystem: Created regular road grid - 6 horizontal and 6 vertical roads (step: 3)
📷 Camera setup: radius=56.0, center=(20, 0, 20)
🌍 Ground created: 60.0x60.0
```

При попытке строить не рядом с дорогой:
```
⚠️ Нельзя строить в (5, 5): рядом нет дороги!
```

## 🎮 Как играть:

1. **Обновите страницу**: `Cmd+Shift+R` (Mac) или `Ctrl+Shift+R` (Windows)
2. **Войдите в игру**
3. **Обратите внимание**:
   - Зеленые клетки = можно строить
   - Серые клетки = нельзя строить
4. **Выбирайте здание** из карточек
5. **Кликайте на зеленую клетку** рядом с дорогой
6. **Постройка автоматически развернется** к ближайшей дороге

## 🎨 Визуальные эффекты:

- ✅ Дороги с текстурой асфальта
- ✅ Непрерывные дороги на перекрестках
- ✅ Зеленые клетки с доступом к дороге
- ✅ Серые клетки без доступа
- ✅ Разметка на дорогах (зебры, пунктирные линии)
- ✅ Компас в правом нижнем углу

## 🚀 Производительность:

- Регулярная сетка **быстрее генерируется** (нет случайных попыток)
- Общий материал для всех дорог = **меньше draw calls**
- Меньше клеток (16x16 vs 32x32) = **быстрее рендеринг**

## ⚙️ Настройка:

### Изменить шаг сетки дорог:
`frontend/src/config/constants.js`:
```javascript
ROAD_GRID_STEP = 3  // Попробуйте 2 или 4
```

- **Шаг 2**: Больше дорог, меньше места для зданий (более плотный город)
- **Шаг 3**: Оптимально (текущее значение)
- **Шаг 4**: Меньше дорог, больше места для зданий (более разреженный город)

### Изменить размер поля:
```javascript
GRID_SIZE = 16  // Попробуйте 12, 20, или 24
```

Камера и дороги автоматически подстроятся!

## 🎉 Готово!

Теперь ваш город имеет:
- ✅ Логичную структуру улиц
- ✅ Гарантированный доступ к каждому зданию
- ✅ Визуальную индикацию доступных участков
- ✅ Правильную ориентацию зданий к дорогам

Стройте свой идеальный город! 🏙️✨


